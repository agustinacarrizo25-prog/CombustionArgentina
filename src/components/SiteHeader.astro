<header class="site-header">
  <div class="site-header-inner">
    <a class="logo" href="/" aria-label="Combustión Argentina inicio">
      <img
        src="/img/brand/combustion-argentina-horizontal-dark.webp"
        width="618"
        height="148"
        alt=""
        decoding="async"
        fetchpriority="high"
      />
    </a>
    <nav aria-label="Principal">
      <a href="#tomos">Tomos</a>
      <a href="#argentinos">Argentinos</a>
      <a href="#proyecto">El proyecto</a>
      <a href="#newsletter">Newsletter</a>
    </nav>
    <a class="header-cta" href="#newsletter">Avisame</a>
  </div>
</header>

<style>
  .site-header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    width: 100%;
    padding: 10px clamp(18px, 3.4vw, 48px);
    border-bottom: 1px solid rgba(200, 169, 81, 0.15);
    background: rgba(10, 10, 10, 0.86);
    backdrop-filter: blur(12px);
  }

  .site-header-inner {
    position: relative;
    display: flex;
    align-items: center;
    gap: 24px;
    width: 100%;
    max-width: 1440px;
    margin: 0 auto;
  }

  .logo {
    display: inline-flex;
    align-items: center;
    width: 330px;
    height: 64px;
    text-decoration: none;
  }

  .logo img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: contain;
    object-position: center;
  }

  nav {
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    align-items: center;
    gap: 28px;
  }

  nav a,
  .header-cta {
    color: var(--brand-muted);
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 2px;
    text-decoration: none;
    text-transform: uppercase;
  }

  nav a:hover {
    color: var(--brand-gold);
  }

  .header-cta {
    margin-left: auto;
    padding: 10px 18px;
    background: var(--brand-gold);
    color: var(--brand-black);
  }

  @media (max-width: 1080px) {
    nav {
      display: none;
    }
  }

  @media (max-width: 820px) {
    .site-header {
      padding: 10px 18px;
    }

    .logo {
      width: 220px;
      height: 48px;
    }
  }
</style>
